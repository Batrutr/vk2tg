services:
  vk-to-telegram-bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: vk2tgbot:v1.3
    container_name: vk-to-telegram-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data.json:/app/data.json
      - ./chats.json:/app/chats.json